---
# StorageClass that ties the LUKS CSI driver to Rook-Ceph (RBD).
# Rook-Ceph must be installed and ready before applying this file.
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: luks-encrypted-ceph
provisioner: luks.csi.example.com
reclaimPolicy: Delete
volumeBindingMode: Immediate
parameters:
  backingStorageClass: rook-ceph-block
  luksType: luks2
  filesystem: ext4

---
# PVC that requests an encrypted volume via our CSI driver (Ceph-backed).
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ceph-test-pvc
  namespace: default
spec:
  storageClassName: luks-encrypted-ceph
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 1Gi

---
# Test pod that writes a file to the encrypted mount and then sleeps.
apiVersion: v1
kind: Pod
metadata:
  name: luks-ceph-test-pod
  namespace: default
spec:
  containers:
    - name: app
      image: alpine:3.19
      command:
        - sh
        - -c
        - |
          echo 'hello from luks+ceph' > /mnt/data/test.txt
          cat /mnt/data/test.txt
          sleep 3600
      volumeMounts:
        - name: encrypted-vol
          mountPath: /mnt/data
  volumes:
    - name: encrypted-vol
      persistentVolumeClaim:
        claimName: ceph-test-pvc
